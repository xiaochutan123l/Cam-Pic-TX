1. Modify Makefile to adapt separate include and source folders.
2. write interfaces:
        - udp_client
        - udp_server
        - camera_module
3. implement interface:
        - udp_client
        - udp_server
        - camera_module
4. implement sender.cpp as new main file.
5. implement latency test. (test_helper class and test_main file)

--place header or helper files like packet_handler and xxx_config to include folder.

--classify code and add namespace